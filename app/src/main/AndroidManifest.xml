<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Launcher">
        <activity
            android:name=".SoundtrackActivity"
            android:configChanges="uiMode|orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout|density"
            android:label="@string/title_activity_soundtrack"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".TransitionActivity"
            android:configChanges="uiMode|orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout|density"
            android:label="@string/set_transition"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EditorActivity"
            android:configChanges="uiMode|orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout|density"
            android:label="@string/photo_editor"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SettingsActivity"
            android:configChanges="uiMode|orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout|density"
            android:label="@string/slideshow_settings"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AboutActivity"
            android:configChanges="uiMode|orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout|density"
            android:label="@string/title_activity_about"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SlideshowActivity"
            android:configChanges="uiMode|orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout|density"
            android:label="@string/title_activity_slideshow"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme"
            android:configChanges="uiMode|keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout|density"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="content" />
                <data android:host="*" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/x-present" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="content" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.present" />
            </intent-filter>
        </activity>
    </application>

</manifest>